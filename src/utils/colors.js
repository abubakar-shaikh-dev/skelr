import chalk from "chalk";

// Minimal color palette
export const c = {
  dim: chalk.dim,
  bold: chalk.bold,
  cyan: chalk.cyan,
  green: chalk.green,
  red: chalk.red,
  yellow: chalk.yellow,
  white: chalk.white,
};
